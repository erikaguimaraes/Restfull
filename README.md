#  Atividade de Restfull

RESTful utilizando Node.js, Express e Prisma, que realize
operações CRUD em usuários, onde os usuários devem possuir um id
(autoincremental), nome , e email (único). Onde os  dados estão persistidos em um
banco de dados.

### 🔧 Instalação
npm install express :instalando o express

npm install prisma --save-dev: intalando o prisma

npx tsc --init : inicializando o type script

npx prisma migrate dev --name init: criando as migrates

Noen: criação de banco de dados

npx tsx server.ts: roda o terminal


## ✒️ Autores

* Erika Costa Guimaraes da Silva










